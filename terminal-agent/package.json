{
	"name": "terminal-agent",
	"version": "1.0.0",
	"description": "A terminal-capable agent that can run shell commands and use the results to complete tasks",
	"type": "module",
	"scripts": {
		"start": "tsx index.ts",
		"build": "tsc",
		"watch": "tsc --watch",
		"clean": "rimraf dist"
	},
	"dependencies": {
		"@mozaik-ai/core": "^3.13.0",
		"dotenv": "^17.2.3"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"rimraf": "^5.0.5",
		"tsx": "^4.21.0",
		"typescript": "^5.3.3"
	}
}
